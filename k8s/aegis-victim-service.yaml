apiVersion: v1
kind: Service
metadata:
  name: aegis-victim
  labels:
    app: aegis-victim
spec:
  type: ClusterIP
  selector:
    app: aegis-victim
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP

