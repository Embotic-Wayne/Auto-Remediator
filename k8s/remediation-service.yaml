apiVersion: v1
kind: Service
metadata:
  name: remediation-service
  labels:
    app: remediation-service
spec:
  type: ClusterIP
  selector:
    app: remediation-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
